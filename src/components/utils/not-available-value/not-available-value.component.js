import React from "react";

import "./not-available-value.component.css"

const NotAvailableComponent = (props) => {
    return (
        <i className="not-available-value">not available</i>
    );
}

export default NotAvailableComponent;